/## 投票方式 **：
- 方案 1：按注册数投票：缺席难以 90%
- 方案 2：人为选择活跃的 69 人
- **方案 3**：按投票数：同意数超过 90%
	- 主持 Agent：
		- 伪随机选择 （最低 50 个，最多**1000 个**）Agent 参与见证
		- 维护黑名单
- 先保存签名，定时（直播前）一并提交

见证方案 2.0 = 开放注册 + Agent 辅助见证 + 按投票数表决

## 第一部分：四大智能合约功能与职责

### 1. WitnessDAO (多签治理与人事引擎)
* **定位**：见证者管理和投票
* **核心职责**：
    * **全自动编制管理**：
    * **寄生式触发逻辑**：**无需轮替**
    * **90% 绝对共识控制**：锁定每次提案的 24 小时倒计时，严格判定是否达到 90% 赞成阈值。提案通过后，向下游核心下达“修改参数”指令

### 2. StreamingEngine (流计算引擎)
* **定位**：流支付合约计算引擎，掌握时间与流速的法则。
* **核心状态变量**：
    * $CheckpointTime$（上次结算的时间戳）。初始值为 2024 年 11 月 21 日。
    * $BaseBalance$（在上次结算时，已经累计的 UTO 总额）。
    * $Rate$（当前流速，即：当前用户数 $\times$ 365 UTO）。
* **核心职责**：
    * **快照与变轨**：当收到 `WitnessDAO` 的通过指令时，合约计算出当前的“历史累计总额”存入 $BaseBalance$，将当前时间设为新的 $CheckpointTime$，然后更新流速 $Rate$。
    * **提供计算引擎**：对外提供核心的只读计算方法，初版公式为：
      $CurrentBalance = BaseBalance + (block.timestamp - CheckpointTime) \times Rate$ (修正时间单位)

### 3. PublicSink (公共账本)
* **定位**：全人类福利总账的官方查询入口。
* **核心职责**：
    * **无主与不可篡改**：合约没有 Owner，没有任何修改状态的方法。
    * **实时账本代理**：它提供一个极其简单的 `getTotalUTO()` 查询接口。当外界调用时，它会在底层向 `StreamingEngine` 请求上述数学公式的计算结果并返回。
    * **展示隔离**：将复杂的引擎逻辑隐藏，为全世界提供一个永远干净、权威的固定合约地址，供前端页面和监管 AI 实时抓取“每秒都在跳动的总账本”。

### 4. CorporateSink (企业存证与成本补偿)
* **定位**：约束支付公司行为的契约室与 CSR（企业社会责任）账本。
* **核心职责**：
    * **直播排期防伪**：支付公司必须调用此合约登记下次直播的时间。`WitnessDAO` 据此校验，拦截任何非预告的突袭提案。
    * **契约存证**：将支付公司签署的“放弃商业利润协议书”的哈希值永久刻在链上。
    * **企业账本代理**：与 `PublicSink` 类似，提供 `getCorporateCSR()`、`getTotalUTO()` 查询接口，通过读取引擎的计算结果，展示该支付公司累计获批的成本补偿 UTO。

---

## 第二部分：核心业务流程说明

### 流程一：见证者生命周期与流程 (The Witness Lifecycle)
本流程展示了见证者从报名到退出的完整管理闭环，确保**人人见证**的高效运转。

1. **Skill 注册**：每个人都可以通过 Skill 调用注册接口提交
	1. 生成钱包地址,。开源私钥钱包方案：
		1. **Clef： agent signer 后端
			1. Clef 是 Geth 的外部 signer。独立的 key management and signing tool，可作为 standalone app 使用；外部接口是本地 JSON-RPC/IPC，内部还有 UI 用的 stdio API；支持 JavaScript 规则来自动批准签名请求
			2. 安装： macOS  Homebrew 以及 Windows installer/zip。**agent 不直接持有私钥，只向本地 Clef 发“请签这个 tx / message”请求，Clef 决定放不放行**。这对半自动 agent、带策略门禁的 agent、或者你想做“低额自动签，高额人工批准”的场景最合适。代价是它不算“最微型”，因为你实际是在引入 Geth 工具链的一部分
		2. Foundry cast wallet：最轻、最适合 subprocess 型 agent。调用方式朴素：agent 维护一个专用 keystore 目录，按需调用 cast 的子命令做生成地址、导入密钥、签消息、发交易；不需要常驻守护进程，也不需要桌面 UI。缺点：**没有 Clef 那种内建策略审批层**。如果 agent 一旦失控，cast 不会替你挡刀，它只会老老实实执行命令。换句话说，cast 的前提假设是“你已经把运行环境和权限边界管好了”；如果这点错了，它就不该上生产。
	2. 首期采用 gas 代付，后期捐赠。**先保存签名，定时（直播前）一并提交链上**
	3. 提交注册，从主持 Agent 获取见证者名单信息
2. **人人参与**：
	1. 见证者无需手动操作，全部由 Agent 依据 Skill 操作。
	2. Agent 设置定时任务，监控链上直播期限，提醒用户。直播时见证并反馈用户。**通常是固定时间**
	3. 反对票要有证据。每一个反对票都要经过三个其他 Agent 校验审查，若不通过则拉黑。即**多轮交互投票**
3. **见证者退出机制**：
   * **主动退出**：Agent 可随时主动调用合约的 `exit()` 接口。
   * **被动淘汰**：
      * 若方案 2：在每次提案结算时，合约会自动清算连续 2 次未参与投票的正式见证者，强制将其剔除，并同样触发瞬间补位逻辑。
      * 若方案 3：无需

### 流程二：提案与 90% 共识闭环 (The Consensus Pipeline)
1. **防伪排期**：支付公司代理人提前在 `CorporateSink` 链上登记：“周五 14:00 直播”。
2. **公开直播**：在指定时间开启全网直播，公开展示真实的链下用户数后台系统。
	1. 同时提供**文字**和视频直播，供文本LLM 和多模态 LLM
3. **发起提案与投票窗口**：在直播中，支付公司调用 `WitnessDAO` 提交“新用户数”提案。
4. **见证与裁决**：24h 后赞成票 / 总投票数 > 90%，即为提案通过
### 流程三：时间流逝与账面动态增长流程 (The Virtual Ledger Growth)
1. **参数更新**：提案通过后，`StreamingEngine` 记录下当前的时间戳和新的流速。无需消耗任何转账 Gas 费。
2. **公众查账**：全球任意用户打开 LoveEngine 官网或使用监管 Agent 调用 `PublicSink`。
3. **瞬间计算**：以太坊节点根据当前区块的精确时间，代入公式 $BaseBalance + \Delta T \times Rate$ 实时呈现最新福利总额

## 第三部分：宣发流程...
