# Current source constraints

These documents are preserved source material. Developer-facing requirements are normalized in `docs/specs/` and `docs/api/`; conflicts and implementation status must be resolved there.
